---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by LJ095.
--- DateTime: 2019/6/14 16:18
---

local UICommonCostTipModel = BaseClass("UICommonCostTipModel", UIBaseModel);
local base = UIBaseModel;

local function OnCreate(self)
    base.OnCreate(self)

end
--_closeCallback 关闭，取消回调
--_certainCallback 确定按钮回调
--_itemId 消耗物品id
--_num 消耗数量
--_info 提示信息
--_tipTogglecallback 提示按钮回调
local function OnEnable(self,_closeCallback,_certainCallback,_itemId,_num,_info,_tipTogglecallback)
    self.closeCallback=_closeCallback;
    self.certainCallback=_certainCallback;
    self.tipTogglecallback=_tipTogglecallback;
    local xlsxItemData=DataUtil.GetDataByIdAndType(_itemId);
    if xlsxItemData~=nil then
        self.itemIcon=xlsxItemData.icon;
    end
    self.itemId=_itemId;
    self.tipInfo=string.format("消费<color=#ffffff00><quad size=20 x=0 y=0 width=3 /></color>x%s%s",_num,_info)
end

UICommonCostTipModel.OnCreate=OnCreate;
UICommonCostTipModel.OnEnable=OnEnable;
return UICommonCostTipModel